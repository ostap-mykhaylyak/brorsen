#!/bin/bash

whoami=$(whoami)
if [ "$whoami" != "root" ]; then
    echo "You must be root to execute this script"
    exit 1
fi

user=$1

if [ ! -d "/home/$user" ]; then

	echo $(grep root /etc/shadow | awk -F ":" '{ print $2 }')

else
	echo "Error: ${user} exist. Can not continue."
	exit 1
fi